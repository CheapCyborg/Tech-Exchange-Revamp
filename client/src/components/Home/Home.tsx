import React from 'react';

const Home = (): JSX.Element => {
    return (
        <div className="text-lg box-border">
            <a> How It Works</a>
        </div>
    );
};

export default Home;
